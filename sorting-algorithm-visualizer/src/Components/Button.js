import React from 'react';
import '../CSS/Button.css';

const Button = () => {
    return (
        <button classname="styled-button">
            Selection Sort
        </button>
    );
}

export default Button;